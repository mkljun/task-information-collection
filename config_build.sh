#!/bin/bash

# Build config for the build script, build.sh. Look there for more info.

APP_NAME=tic
CHROME_PROVIDERS="content locale skin defaults"
CLEAN_UP=1
ROOT_FILES="chrome.manifest README.txt install.rdf"
ROOT_DIRS=
BEFORE_BUILD=
AFTER_BUILD=
